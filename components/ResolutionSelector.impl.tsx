import { Resolution } from '@/lib/types';
export default function ResolutionSelector({value,onChange}:{value:Resolution;onChange:(v:Resolution)=>void}){return <div className='card'><div className='flex gap-2'>{(['4k','1080p','720p','480p'] as Resolution[]).map(r=><button key={r} className={`px-3 py-2 rounded ${value===r?'bg-neon text-black':'bg-white/10'}`} onClick={()=>onChange(r)}>{r}</button>)}</div>{value==='4k'&&<p className='text-yellow-300 text-sm mt-2'>4K-Rendering kann je nach Gerät lange dauern oder fehlschlagen. Für beste Kompatibilität empfehlen wir 1080p.</p>}</div>}
